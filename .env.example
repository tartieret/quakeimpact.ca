# The GA4 property this build reports to. Read at build time and inlined into
# the export, so it is the Netlify build environment that needs it, not the
# running site. Leave it unset and the site loads no analytics at all.
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
